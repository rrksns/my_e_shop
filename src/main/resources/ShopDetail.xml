<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ShopDetailns">
	<resultMap id="shopDetailResult" 		type="shopDetail">
	<!-- 	<result property="a" 	column="a" />
		<result property="b" 	column="b" />
		<result property="c"		column="c" /> -->
	</resultMap>
	
	<!-- <select id="list" resultMap="sellerResult">
		select * from test 
		</select> -->
	
<!-- 	<select id="select" parameterType="int" resultType="dept"> 데이터한건일때 resultType
		select * from dept where deptno=#{deptno}
	</select> -->
	
<!-- 	<update id="update" parameterType="shop">
		update shop set sh_img1= #{sh_img1}, sh_img2=#{sh_img2}, 
			sh_img3=#{sh_img3},sh_img4= #{sh_img4},
			sh_img5= #{sh_img5}, sh_img6= #{sh_img6}
			where sh_id=#{sh_id}
	</update> 
	 -->
	<!-- <delete id="delete" parameterType="int">
		delete from dept where deptno=#{deptno}
	</delete> -->
	<insert id="insert" parameterType="shopDetail">
	<selectKey keyProperty="sh_detailId" order="BEFORE" resultType="int">
		select ifnull(max(sh_detailId), 0)+1 sh_detailId from shop
	</selectKey>
			insert into shop (sh_detailId, allday_open, driveThru, takeOut, dineIn,
			delivery, aloneMeal, parking, parkingSpace, toilet, smoking, mobileCharge,
			blanket, kidsroom, freeWifi) 
			values(#{sh_detailId}, #{allday_open}, #{driveThru}, #{takeOut}, #{dineIn}, 
			#{delivery}, #{aloneMeal}, #{parking}, #{parkingSpace}, #{toilet}, 
			#{smoking}, #{mobileCharge}, #{blanket}, #{kidsroom}, #{freeWifi}, #{sh_id} )
	</insert>
	<select id="select" parameterType="string" resultType="shop">
		select * from shop where sh_detailId = 
		(select ifnull(max(sh_detailId), 0) from shopDetail)
	</select>
</mapper>