<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Shopns">
	<resultMap id="shopResult" 		type="shop">
	<!-- 	<result property="a" 	column="a" />
		<result property="b" 	column="b" />
		<result property="c"		column="c" /> -->
	</resultMap>
	
	<!-- <select id="list" resultMap="sellerResult">
		select * from test 
		</select> -->
	
<!-- 	<select id="select" parameterType="int" resultType="dept"> 데이터한건일때 resultType
		select * from dept where deptno=#{deptno}
	</select> -->
	
	<update id="update" parameterType="shop">
		update shop set sh_img1= #{sh_img1}, sh_img2=#{sh_img2}, 
			sh_img3=#{sh_img3},sh_img4= #{sh_img4},
			sh_img5= #{sh_img5}, sh_img6= #{sh_img6}
			where sh_id=#{sh_id}
	</update> 
	
	<!-- <delete id="delete" parameterType="int">
		delete from dept where deptno=#{deptno}
	</delete> -->
	<insert id="insert" parameterType="shop">
	<selectKey keyProperty="sh_id" order="BEFORE" resultType="int">
		select ifnull(max(sh_id), 0)+1 sh_id from shop
	</selectKey>
			insert into shop (sh_id, sh_name, sh_addr, sh_tel,sh_operTime,
			sh_brTime, s_id) 
			values(#{sh_id}, #{sh_name}, #{sh_addr}, #{sh_tel}, #{sh_operTime}, 
			#{sh_brTime}, #{s_id} )
	</insert>

</mapper>